S-89 next bounded chunk: prepare the Treehouse layers from the attached master and integrate the exterior locally for review.

Olive integration has passed at 1592a98edbee267b13da4e8a36e977f347e40740 (tree c03af1f66fa2a0a108bac3255b908ebfb31b5406). Preserve all candidate history and any later status/docs commits.

Read README.md and inspect candidate/treehouse-master-1024.png. This is the complete warm painterly transparent Treehouse candidate, with full canopy and no baked-in characters. It is NOT already an aligned layer pair. The file reference/foreground-intent-UNREGISTERED.png is only a selection-intent study: image generation moved its rail/root edges. Never load it at runtime or use its geometry as if it were registered.

1. Retain originals outside runtime paths. Create deterministic back/front layers from the SAME 1024 master, using a trace/mask against its actual pixels. Use existing image tools (for example a traced SVG/polygon selection rasterized with sharp), not new generative artwork. Preserve pixel colors and geometry. Select only the near/viewer-right stair rail/posts/fascia, nearest root and lowest foreground leaves for front; keep the cabin/canopy/trunk/balcony/stair treads behind. Do not use a broad rectangular cut. If exact tracing cannot be completed faithfully, return the concrete incomplete mask and limitation instead of claiming a finished pair.

2. Export treehouse.webp and treehouse-front.webp on identical untrimmed 1024 canvases, with a COMMON anchor/scale derived from the master. Recompose and compare to the master with no character inserted; verify alpha and visible RGB equivalence and inspect seams. Keep the extraction script/mask reproducible. Never independently auto-centre or trim the front layer.

3. Integrate the pair in the existing Treehouse exterior consumers, including map and arrival where applicable. Preserve Treehouse location, 5x5 footprint, interactions and accepted EC-1/EC-2/Olive behavior. Update measured content metadata. Start from the existing scale and make a small evidence-based size adjustment only if needed. The new master bbox anchor is a suggestion, not an entry coordinate. Demonstrate correct Friend occlusion near the stairs/root; keep front-layer depth local to the relevant scene rather than making it globally topmost. Do not expand global navigation or travel mechanics.

4. Return one local review candidate with incremental diff, SHA/tree, relevant typecheck/build/test results and a downloadable ZIP under 30 MiB. Include phone, tablet and desktop map/arrival views, a small near-stair view that shows the mask boundary, and a short selected-Friend occlusion demonstration in front of/behind the intended pieces. Assess whole-canopy overview framing, stair-ground contact, landmark/character proportions and readable doorway. Label viewport simulation versus real-device testing. Assess the lookout's shelter against the approved reference; this master puts the telescope on an open balcony section, so do not silently claim perfect architectural fidelity.

No need for an extra planning or audit stage. Stop at this Treehouse integration checkpoint. No EC-3 expansion, other-guide rollout, boat implementation, Doug-cycle rollout, push, merge or deployment. No new S-ID. Final visual acceptance awaits the in-context review.
